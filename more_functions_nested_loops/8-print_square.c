#include "main.h"

/**
*print_square - Draws square.
*@size: size of the square
*
*Return: void.
*/
void print_square(int size)
{
	int h, w;

	if (n > 0)
		for (h = 0; i < size; i++)
			for (w = 0; w < size; w++)
				_putchar('#');
			_putchar('\n');
	else
		_putchar('\n');
}
